# Installed Python from Python.org while installation added to path.

# Python.org ( only one community to mentain ) - 
    # same behaviour if we run anywhere like Js which has different behaviour in browser and node.

# For windows use gitbash (in place of command promt) otherwise powershell. 

# python --version      -       Less breaking changes and more backward compactability.

# Interpeted orCompiled

print("Chai aur Python") 

def chai(n):
    print(n)

chai(4)
chai("Milk Tea")